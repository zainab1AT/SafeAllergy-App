name: safe_allergy
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  flutter_nfc_kit: ^3.6.1 # remove nfc_manager / nfc_manager_ndef if using this
  mobile_scanner: ^7.1.3
  multi_select_flutter: ^4.1.3
  encrypt: ^5.0.3
  crypto: ^3.0.7
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0
  google_fonts: ^6.3.2
  qr: ^3.0.2
  permission_handler: ^12.0.1
  image_picker: ^0.9.10+1 # update to current
  image_gallery_saver: ^2.0.3
  google_mlkit_barcode_scanning: ^0.13.0
  cloud_firestore: ^6.1.0
  firebase_auth: ^6.1.2
  firebase_core: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  mocktail: ^1.0.3
  fake_cloud_firestore: ^4.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
